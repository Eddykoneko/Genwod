{% extends 'base.html.twig' %}

{% block title %}Favoris{% endblock %}

{% block body %}
    <h1>Favoris</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favori.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_favoris_index') }}">back to list</a>

    <a href="{{ path('app_admin_favoris_edit', {'id': favori.id}) }}">edit</a>

    {{ include('admin_favoris/_delete_form.html.twig') }}
{% endblock %}
