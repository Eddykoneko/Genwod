<form method="post" action="{{ path('app_admin_favoris_delete', {'id': favori.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effectuer cette action?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favori.id) }}">
    <button class="btn-primary">Delete</button>
</form>
