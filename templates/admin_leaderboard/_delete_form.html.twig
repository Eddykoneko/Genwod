<form method="post" action="{{ path('app_admin_leaderboard_delete', {'id': leaderboard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leaderboard.id) }}">
    <button class="btn-primary">Delete</button>
</form>
