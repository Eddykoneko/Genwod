{% extends 'base.html.twig' %}

{% block title %}Commentaire index{% endblock %}

{% block body %}
    <h1>Commentaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Date de création</th>
                <th>Exercice</th>
                <th>Email</th>
                <th>Commentaire</th>
                <th>CreatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                <td>{{ commentaire.exerciceId.createdAt|date('d-m-Y') }}</td>
                <td>{{ commentaire.exerciceId.description }}</td>
                <td>{{ commentaire.getUser_email }}</td>
                <td>{{ commentaire.commentaire }}</td>
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_admin_commentaire_show', {'id': commentaire.id}) }}" class="btn btn-primary">show</a>
                    <a href="{{ path('app_admin_commentaire_edit', {'id': commentaire.id}) }}" class="btn btn-primary">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_commentaire_new') }}" class="btn btn-primary">Create new</a>
{% endblock %}
