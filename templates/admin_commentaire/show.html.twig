{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ commentaire.getUser_email }}</td>
            </tr>
                <th>Commentaire</th>
                <td>{{ commentaire.commentaire }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_commentaire_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('app_admin_commentaire_edit', {'id': commentaire.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin_commentaire/_delete_form.html.twig') }}
{% endblock %}
